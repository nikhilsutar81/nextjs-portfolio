// Calendar component removed since react-day-picker caused dependency conflicts and is no longer used.
// If you need a calendar in the future, consider using react-datepicker or another library.

// Only kept for reference; you can delete this file entirely.

export const Calendar = () => null;
